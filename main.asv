clc
clear all
close all
addpath('src\');
% [audio,Fs, pos, watermark] = encoder('music\A.mp3');
num = 100;
test_water1= zeros(1,num);
test_water2= zeros(1,num);
for i=1:num
    test_water1 = round(rand);
    test_water2 = round(rand);
end
% size(watermark,2)
% decoder(audio,pos,watermark)
% sound(audio,Fs);
% clear sound;